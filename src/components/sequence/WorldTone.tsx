// WorldTone is now driven directly by SequenceRig's ScrollTrigger onUpdate.
// This stub is kept for backwards-compat; the real color sync happens in SequenceRig.tsx.
export function WorldTone() {
  return null;
}
