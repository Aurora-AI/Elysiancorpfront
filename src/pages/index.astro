---
import Layout from '../layouts/Layout.astro';
import { GazeForensicHero } from '../components/hero/GazeForensicHero';
import { MagneticCursor } from '../components/cursor/MagneticCursor';
import { Header } from '../components/ui/Header';
import { SequenceRigFull } from '../components/sequence/SequenceRigFull';
import { GlobalNoiseCanvas } from '../components/sequence/GlobalNoiseCanvas';
---

<Layout title="ElysianCorp | Sovereign Intelligence Architecture">
	<GlobalNoiseCanvas client:only="react" />
	<MagneticCursor client:load />
	<Header client:load />

	<main>
		{/* SEÇÃO 1: STOP-MOTION ORIGINAL (GAZE FORENSIC V1.1.0) */}
		<GazeForensicHero client:load />

		{/* CINEMATIC SEQUENCE — client:only skips SSR entirely, preventing
		    Suspense streaming corruption and hydration mismatch with the
		    500vh spacer and position:fixed CameraStage */}
		<SequenceRigFull client:only="react" />
	</main>
</Layout>

<style>
	main {
		width: 100%;
		min-height: 100vh;
	}
</style>
